CampaignList.js
import { useHistory } from 'react-router';

let history = useHistory();

history.push(getRowLink(rowData));

function getRowLink(rowData) {
  return './campaign/' + rowData._id;
}

useHistory: redirect to another page.
Match the routes, routes to the server editor

Next step:
Input 
Working on the editor
Use campaign inputs

data variable information
use them create one survey object on Editor page.  
Pop up to your inputs and modify it. 
Create a function handleSave to console.log the object.




Work Progress

SurveyList - handleSearch function
HeaderRevised - button changes

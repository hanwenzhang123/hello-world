   import Autocomplete, {
  createFilterOptions
} from '@material-ui/lab/Autocomplete';

               <Autocomplete
                    className={classes.rootBottomInput}
                    multiple
                    limitTags={1}
                    size="small"
                    id="checkboxes"
                    options={regions}
                    disableCloseOnSelect
                    getOptionLabel={option => option.title}
                    value={audienceObj}
                    onChange={handleAudienceList}
                    renderOption={(option, { selected }) => (
                      <React.Fragment>
                        <Checkbox
                          icon={icon}
                          checkedIcon={checkedIcon}
                          style={{ marginRight: 8 }}
                          checked={selected}
                        />
                        {option.title}
                      </React.Fragment>
                    )}
                    renderInput={params => (
                      <TextField
                        {...params}
                        variant="outlined"
                        label="Audience Region"
                        placeholder="Select or search"
                        required
                      />
                    )}
                    disabled={disableStatus}
                  />


4 components
- Single selected
- Multiple selected
- Date
- Number
